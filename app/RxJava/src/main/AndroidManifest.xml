<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asw.rxjava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Interview">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.RxJavaCreate" />
        <activity android:name=".activity.RxJavaTransform" />
        <activity android:name=".activity.RxJavaFilter" />
        <activity android:name=".activity.RxJavaCombining" />
        <activity android:name=".activity.RxJavaError" />
        <activity android:name=".activity.AncillaryActivity" />

    </application>

</manifest>